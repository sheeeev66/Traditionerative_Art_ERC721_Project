const TraditionerativeArt = artifacts.requre("TraditionerativeArt");
contract("TraditionerativeArt", (accounts) => {
    let [alice, bob] = accounts;
    it("should be able to mint a new token", async () => {
    })
})